import PageLoader from "@/components/PageLoader";
import HomeSection from "@/components/HomeSection";

const Home = () => (
  <PageLoader>
    <HomeSection />
  </PageLoader>
);

export default Home;
